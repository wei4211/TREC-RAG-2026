# 版本快照

每個資料夾是該版本的 `src/` 和 `scripts/` 實際檔案,從對應的 git 標籤匯出。
**主資料夾(上一層)永遠是最新版**;這裡是為了方便直接翻閱和比較舊版。

| 資料夾 | 標籤 | 內容 |
|---|---|---|
| `A-official/` | `version-A-official` | 官方對齊:Llama-3.3-70B、Top-5000 候選池、變深度提交、④LLM 改寫、v0.6.0 metadata |
| `B1-breadth/` | `version-B1-breadth` | + 廣度輪流分配預算、面向 8-12、一句一事實短句、禁止刪句、`run_desc` |
| `B2-nugget-loop/` | `version-B2-nugget-loop` | + 完整度優先(15-25 字)、自評補洞迴圈(逐面向 nugget 預測 → 批改 → 定向補洞)、字數上限事後複查 |
| `B3-enumerative/` | `version-B3-enumerative` | + 列舉式寫法(25-40 字,把每個維度都點名),取代「一句一事實」 |
| `C-freeform-agent/` | `version-C-freeform-agent` | Route B:自由 tool-calling agent(`freeform_agent.ts`),架構對照組,非 A→B 線的延伸 |

主資料夾(`src/`、`scripts/`,上一層)永遠等於最新 commit,和 `C-freeform-agent/` 內容相同 —— Route B 目前就是最新版。

## dev22 實測

### R 任務(檢索,三份官方 qrels 平均;candidate pool,非變深度提交檔)

| 版本 | nDCG@10 | nDCG@20 | nDCG@100 | nDCG@1000 | Recall@100 | Recall@1000 | MAP | MRR |
|---|---|---|---|---|---|---|---|---|
| 學姊 baseline(原封未改) | 0.6411 | 0.6346 | 0.6279 | 0.3072 | 0.0982 | 0.2631 | 0.1435 | 0.9003 |
| A | 0.7475 | 0.7349 | 0.6246 | 0.3365 | 0.0956 | 0.2906 | 0.1960 | 0.9452 |
| B1 | 0.7515 | 0.7342 | 0.6202 | 0.3371 | 0.0944 | 0.2908 | 0.1964 | 0.9552 |
| **B2** | **0.7526** | 0.7324 | 0.6245 | **0.3391** | 0.0953 | **0.2919** | **0.1990** | **0.9703** |
| B3 | 跑測中 | | | | | | | |
| Route B | 未跑 | | | | | | | |

來源:`trec-rag-2026-agentic-rag-baseline-minimal-20260724T170610Z/runs/.../metrics.json`
的 `arithmetic_mean_across_qrels`,逐字抄錄。**修正**:先前這裡誤植了
`trec-rag-2026-no-ce`(Maverick、無CE 的自建對照組)的數字(ndcg_10=0.6543),
不是學姊真正原封未改的版本。

B1/B2 之間的差異都在雜訊範圍內(合理 —— 這兩版只改生成端,沒有動檢索端)。

⚠️ **這份表格評的是完整的重排候選池(`runfile.trec`),不是實際會提交的檔案。**
2026 官方採「變深度」規則(每題自訂 k、不得補滿固定深度),真正要交的是
`submission_variable_k.trec`。用 `scripts/eval_submission.ts` 對提交檔實測:

| | P@k | R@k | nDCG@k | 未判定率 |
|---|---|---|---|---|
| 學姊 baseline | 不適用 —— 這版沒有變深度提交,`submission_variable_k.trec` 不存在 | | | |
| A 變深度提交 | 0.9011 | 0.0165 | 0.7486 | 2.6% |
| B1 變深度提交 | 0.8871 | 0.0164 | 0.7475 | 4.0% |
| **B2 變深度提交(平均 k≈12.5)** | 0.8919 | 0.0168 | 0.7521 | 3.1% |

同一份 B2 排序,改用固定深度會怎樣(對照組):

| 深度 | P@k | R@k | nDCG@k | 未判定率 |
|---|---|---|---|---|
| top-5 | 0.9182 | 0.0070 | 0.7607 | 3.6% |
| top-10 | 0.8970 | 0.0136 | 0.7528 | 3.6% |
| top-15 | 0.8768 | 0.0199 | 0.7427 | 3.9% |
| top-20 | 0.8614 | 0.0257 | 0.7313 | 4.5% |
| top-50 | 0.7876 | 0.0570 | 0.6884 | 7.5% |

**A/B1/B2 三版的變深度提交成績幾乎沒有差異**(合理——三版都只改生成端,
沒有動過檢索或三軌融合),而且都約略等同固定 top-10。目前的信心門檻
(`ce_calibrated≥0.5`)沒有額外拉高分數,變深度的價值在「符合官方規則」
而非「分數更高」。未判定率隨深度加深而上升(top-5 的 3.6% → top-50 的
7.5%)在預期之中:排名越後面,官方 qrels 判過的機率越低。

逐題檢視也抓到一個排序失敗,而且**三版都一樣失敗**(三角驗證,確認是穩定
bug 而非雜訊):topic 515 有 414 篇相關文件,但融合排序前 4 名(該題被壓
到下限 k=4)一篇都沒中；追下去發現 BM25 排名 #0 的正確文件被 CrossEncoder
(#58)和 dense(#51)兩票否決,融合後掉到 #24。這是三軌融合的已知風險
(多數暴力壓過單軌強訊號),留待之後處理,不影響本輪涵蓋線的結論。

### RAG 任務(涵蓋 / 支持,RAGDoll 官方 prompt,gpt-oss-120b 判官)

| 版本 | 涵蓋(寬) | 涵蓋(嚴,主指標) | 加權支持 | hard 支持 |
|---|---|---|---|---|
| 學姊 baseline(原封未改,generator=gpt-oss-120b) | 32.3% | 26.7% | 67.3% | 48.0% |
| 參考:no-ce 對照組(Maverick,非學姊原版) | 16.7% | 11.5% | 75.5% | 68.4% |
| A | 21.9% | 16.0% | 83.1% | 70.3% |
| B1 | 26.1% | 16.7% | 93.0% | 90.5% |
| **B2** | **34.2%** | **25.6%** | 92.7% | 89.5% |
| B3 | 跑測中 | | | |
| Route B | 未跑 | | | |

⚠️ 上面「學姊 baseline」原本標的 16.7%/11.5%/75.5%/68.4% 其實是 `no-ce`
(自建的 Maverick、無CE 對照組)跑出來的,不是學姊原封未改版本的涵蓋率
——`agentic-rag-baseline-minimal` 有 `rag_output_trec_rag_2026.jsonl` 但
從未被 `rag_selfeval.py` 評過。要補真數字得針對它另外跑一次自評,
在此之前保留「待補」,不引用未經評估的數字。

主指標是 strict vital coverage(只有「完整陳述」才計分,partial 算 0)。
官方 2026 RAG 評分只有 nugget 涵蓋 + 系統對打兩項,**沒有獨立的 support 指標**——
support 是及格門檻,涵蓋才是要衝的分數,這也是為什麼上面把嚴格涵蓋標成主指標。
B2 的答案 22 題全部在 1024 字上限內,數字可直接引用。

⚠️ 學姊 baseline 用 gpt-oss-120b 生成,A 開始才換成 Llama-3.3-70B——A 相對
baseline 同時換了模型和架構,不是單一變因的對照。baseline 只用 215 字、9 句
就拿到 strict 26.7%,贏過 A(16.0%)和 B1(16.7%),打平 B2(25.6%),换模型
本身可能就是落差的一大部分,B1→B2 的進步仍然成立,但起跑點被模型拉低了多少
還沒有隔離出來。

參照點:同一批 nuggets 下,本專案早期的 Python 版 v4 拿到 寬 60.9% / 嚴 54.2%,
但 hard 支持只有 46.2%,答案僅 540 字 18.6 句(句長 31.9 字)。TS 線至今最高是
B2 的 25.6%,所以 v4 仍是要追上的目標 —— B3 就是為此而改。

### 標籤分布:三版是一條接力

| | A | B1 | B2 |
|---|---|---|---|
| 完全命中 support | 16.0% | 16.7% | **25.6%** |
| 部分命中 partial | 11.8% | 18.8% | 17.2% |
| 完全沒中 not_support | 72.2% | 64.5% | **57.2%** |

B1 把「完全沒中」轉成「部分命中」但卡在那裡;B2 才把「部分命中」推成「完全命中」。
兩步的機制不同,缺一不可 —— 這也是為什麼 B1 的 strict 幾乎沒動(16.0→16.7%)而 B2 跳了 +8.9pp。

## 改進路徑

每一步都由前一步的數據指出,不是憑空加模組:

```
A    官方對齊,建立基準
 ↓   診斷:句數(28)< 每題 vital nugget 數(41),是結構性天花板;
     且預算被前段面向吃光,後段面向被整段截斷
B1   廣度輪流分配 + 一句一事實
 ↓   結果:hard 支持 70.3% → 90.5%,涵蓋 21.9% → 26.1%
     診斷:漏掉的變成「部分命中」而非「完全命中」(partial 11.8%→18.8%,
     support 只從 16.0%→16.7%),句子 9.6 字太短講不完整
B2   完整度優先(15-25 字)+ 自評補洞迴圈
 ↓   結果:strict 涵蓋 16.7% → 25.6%(+8.9pp),支持守在 ~90%
     診斷:跟早期 Python v4 對照才發現方向錯了 —— gold nugget 是主題性短句,
     沒有數字專名,而且成家族出現(business interests affect 付薪/媒體權/管理;
     X inclusion is a challenge 之於 種族/性別/階級/性向/身障)。一句列舉式
     的話可涵蓋整個家族,而「一句一事實」把每句的上限鎖在 1 條
B3   列舉式寫法:把每個被點名的維度都寫出來
 ↓
C    Route B:改由模型自己決定控制流程,作為架構對照
```

途中修掉的兩個實作缺陷,都是量測時才發現的:

- A 版有一題只寫出 1 句話(涵蓋 0%),原因是預算被前段面向吃光後直接截斷。B1 的輪流分配讓最少的一題也有 63 句。
- B2 首次跑完有 3 題超過官方 1024 字上限(最多 1237 字):字數上限在分配階段有檢查,但之後的 grounded revision 會把句子改長,沒有人再檢查一次。補上事後複查與 `ANSWER_TOO_LONG` 驗證後重跑那 3 題,才得到上表的合規數字。

## 重新產生快照

```bash
git archive <tag> trec-rag-2026-agentic/src trec-rag-2026-agentic/scripts \
  | tar -x -C trec-rag-2026-agentic/versions/<name> --strip-components=1
```
